version: '3'
includes:
  core:
    taskfile: ../core.yml
    internal: true

tasks:
  sdxl-image-encoder-vit-h:
    desc: "sdxl vit-h image encoder"
    deps:
      - task: core:hf
        vars:
          AUTHOR: h94
          REPO: IP-Adapter
          SUB_PATH: models/image_encoder/
          FILENAME: model.safetensors
          RENAME: sdxl_vit-h_image_encoder.safetensors
          DEST: "{{.MODELS}}/clip_vision"    
